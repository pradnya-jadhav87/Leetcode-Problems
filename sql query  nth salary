CREATE FUNCTION getNthHighestSalary(N INT) RETURNS INT
BEGIN
    SET N = N - 1;

    RETURN (
        SELECT salary
        FROM (
            SELECT DISTINCT salary
            FROM Employee
            ORDER BY salary DESC
        ) AS t
        LIMIT N, 1
    );
END
